package Startup

/*(val list = getListUser()

fun getListUsername(): List <String>
{
    val name = mutableListOf<String>()
    for (user in list)
    {
        name.add(user.name)
    }

    return name
} */